<section class="preview-section" aria-labelledby="statement-preview-title">
  <div class="preview-section__inner preview-section__inner--split">
    <div>
      <p class="preview-section__eyebrow">The statement</p>
      <h2 id="statement-preview-title">A framework for questions at cosmic scale</h2>
    </div>
    <div>
      <p>
        Cosmic Universalism brings philosophical inquiry, computational models, and
        long-horizon cosmology into one evolving framework. Its propositions are
        presented as propositions—not as established scientific findings.
      </p>
      <a
        class="preview-section__link"
        href={import.meta.env.BASE_URL + "framework"}
      >
        Read the framework overview
      </a>
    </div>
  </div>
</section>

<style>
  .preview-section {
    padding-block: clamp(var(--space-12), 8vw, 7rem);
    border-top: 1px solid var(--color-translucent-blue);
  }

  .preview-section__inner {
    width: min(100% - (2 * var(--space-6)), 90rem);
    margin-inline: auto;
  }

  .preview-section__inner--split {
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(0, 1.2fr);
    gap: var(--space-12);
  }

  .preview-section__eyebrow {
    margin: 0 0 var(--space-3);
    color: var(--color-amber);
    font-size: 0.875rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  h2 {
    margin: 0;
    color: var(--color-white-gold);
    font-size: clamp(2rem, 5vw, 3.5rem);
  }

  p {
    margin-block: 0 var(--space-6);
  }

  .preview-section__link {
    display: inline-flex;
    min-height: 2.75rem;
    align-items: center;
    font-weight: 700;
  }

  @media (max-width: 48rem) {
    .preview-section__inner--split {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }
  }

  @media (max-width: 36rem) {
    .preview-section__inner {
      width: min(100% - (2 * var(--space-4)), 90rem);
    }
  }
</style>