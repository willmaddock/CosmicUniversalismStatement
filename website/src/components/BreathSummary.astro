<section class="breath-summary" aria-label="Cosmic Breath phase summary">
  <article class="breath-summary__card">
    <h2>Expansion Phase</h2>
    <p class="breath-summary__duration">2.8 trillion years</p>
    <p class="breath-summary__range">sub-ztom → atom</p>
  </article>

  <article class="breath-summary__card">
    <h2>Compression Phase</h2>
    <p class="breath-summary__duration">308 billion years</p>
    <p class="breath-summary__range">btom → ztom</p>
  </article>

  <article class="breath-summary__card">
    <h2>Full Cosmic Breath</h2>
    <p class="breath-summary__duration">3.108 trillion years</p>
    <p class="breath-summary__range">Expansion + Compression</p>
  </article>
</section>

<style>
  .breath-summary {
    display: grid;
    width: 100%;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--space-4);
  }

  .breath-summary__card {
    min-width: 0;
    padding: var(--space-6);
    border: 1px solid var(--color-translucent-blue);
    border-radius: 0.25rem;
    background: var(--color-surface-translucent);
  }

  .breath-summary__card h2 {
    margin: 0;
    color: var(--color-white-gold);
    font-size: 1.125rem;
  }

  .breath-summary__duration {
    margin-block: var(--space-4) 0;
    color: var(--color-cyan);
    font-size: clamp(1.25rem, 2.5vw, 1.75rem);
    font-weight: 700;
    line-height: 1.3;
  }

  .breath-summary__range {
    margin-block: var(--space-2) 0;
    color: var(--color-text-muted);
    overflow-wrap: anywhere;
  }

  @media (max-width: 48rem) {
    .breath-summary {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 36rem) {
    .breath-summary__card {
      padding: var(--space-4);
    }
  }
</style>
